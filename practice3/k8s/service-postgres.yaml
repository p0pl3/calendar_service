apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: calendar
  annotations:
    config.linkerd.io/opaque-ports: "5432"
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
